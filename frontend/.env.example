# Base URL of the Axum backend API. Copy this file to `.env` and adjust as needed.
# Must be prefixed with PUBLIC_ to be exposed to the browser in SvelteKit.
PUBLIC_API_BASE_URL=http://localhost:3000
